# =============================================================================
# SWaT PyTorch CNN+BiLSTM Ensemble — Python Dependencies
# =============================================================================
# Python : 3.10.x
# PyTorch: 2.1.2 CPU (GT 730 CC3.5 unsupported in PyTorch>=1.5 CUDA)
# For GPU (better than GT730): pip install torch==2.1.2+cu118 --index-url https://download.pytorch.org/whl/cu118
# =============================================================================

# ── PyTorch (CPU build — GT 730 compatible) ───────────────────────────────────
torch==2.1.2
torchvision==0.16.2

# ── Core scientific stack ─────────────────────────────────────────────────────
numpy==1.26.4            # no numpy<1.24 constraint (TF removed)
scipy==1.11.4
pandas==2.1.4
scikit-learn==1.3.2

# ── ML libraries ──────────────────────────────────────────────────────────────
xgboost==2.0.3           # CPU mode
shap==0.43.0
imbalanced-learn==0.11.0
joblib==1.3.2

# ── Plotting ──────────────────────────────────────────────────────────────────
matplotlib==3.7.5
seaborn==0.13.2

# ── Utilities ─────────────────────────────────────────────────────────────────
packaging==23.2
six==1.16.0
python-dateutil==2.8.2
pytz==2024.1
tzdata==2024.1
charset-normalizer==3.3.2
certifi==2024.2.2
idna==3.6
urllib3==2.1.0
requests==2.31.0

# ── ICS / Modbus (attack injection — optional) ────────────────────────────────
pymodbus==3.6.9
pyserial==3.5
websockets==12.0
aiohttp==3.9.3

# ── Cryptography ──────────────────────────────────────────────────────────────
cryptography==42.0.5
cffi==1.16.0
pycparser==2.21

# ── Jupyter ───────────────────────────────────────────────────────────────────
jupyter==1.0.0
ipykernel==6.29.0
nbformat==5.9.2

